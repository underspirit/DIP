function changeGrayScale(a,p)
   [m, n] = size(a);
   q = 256/p;
   for i = 1:m
       for j = 1:n
           for k = 0:q
               if(a[m,n] >= p*q)
                   a(m,)
               end
           end
       end
   end
end